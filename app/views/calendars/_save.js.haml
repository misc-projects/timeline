- if @calendar.errors.any?
	- @calendar.errors.each do |message|
		$('input[name=\'calendar[' + "#{ message }" + ']\']').addClass('has-error');
- else
	$("#form-modal").modal("hide")


- # use this instead: https://www.alfajango.com/blog/rails-3-remote-links-and-forms/